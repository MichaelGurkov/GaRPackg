
\usepackage{tikz}

\usetikzlibrary{shapes,arrows,shadows, positioning}

\usetikzlibrary{patterns, backgrounds}


\tikzset{Header Style/.style={rectangle, rounded corners, shade, top color=white,
		bottom color=blue!20, draw=blue!40!black!60,
		very thin, align=center,font=\LARGE, minimum size=3em}}


\tikzset{Item Style/.style={rectangle, rounded corners, blue!20!black!80,
		draw=blue!40!black!60, very thin, align=center,
		minimum size=3em, text width=65pt}}

